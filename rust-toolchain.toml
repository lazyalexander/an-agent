# Pinned toolchain. Upgrade discipline: one minor at a time; after bumping,
# run `cargo check/clippy/test --all-targets` and fix new lints in the same
# commit.
[toolchain]
channel = "1.98.0"
components = ["clippy", "rustfmt"]
